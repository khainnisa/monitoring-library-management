stages:
  - test
  - deploy

variables:
  MYSQL_ROOT_PASSWORD: rootpassword
  MYSQL_DATABASE: library_db
  MYSQL_USER: laravel
  MYSQL_PASSWORD: password
  DB_CONNECTION: mysql
  DB_HOST: mysql
  DB_PORT: 3306
  DB_DATABASE: library_db
  DB_USERNAME: laravel
  DB_PASSWORD: password

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - vendor/

before_script:
  - apt-get update -yqq
  - apt-get install -yqq git libzip-dev libpng-dev libonig-dev libxml2-dev unzip curl default-mysql-client
  - docker-php-ext-install pdo_mysql mbstring zip exif pcntl bcmath gd
  - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
  - composer install --no-interaction --prefer-dist --optimize-autoloader

test:unit:
  image: php:8.2-fpm
  stage: test
  services:
    - name: mysql:8.0
      alias: mysql
      command:
        ["--default-authentication-plugin=mysql_native_password", "--skip-ssl"]
  script:
    - echo "Waiting for MySQL to be ready..."
    - |
      for i in {1..30}; do
        if mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "SELECT 1" &>/dev/null; then
          echo "MySQL is ready!"
          break
        fi
        echo "Waiting for MySQL..."
        sleep 2
      done
    - echo "Setting up database and user..."
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "CREATE DATABASE IF NOT EXISTS library_db;"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "DROP USER IF EXISTS 'laravel'@'%';"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "CREATE USER 'laravel'@'%' IDENTIFIED WITH mysql_native_password BY 'password';"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "GRANT ALL PRIVILEGES ON *.* TO 'laravel'@'%' WITH GRANT OPTION;"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "FLUSH PRIVILEGES;"
    - echo "Verifying user was created..."
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "SELECT User, Host, plugin FROM mysql.user WHERE User='laravel';"
    - echo "Testing laravel user connection..."
    - mysql -hmysql -ularavel -ppassword --ssl=0 -e "SHOW DATABASES;"
    - echo "Setting up Laravel environment..."
    - rm -f .env
    - echo "APP_NAME=LibraryManagement" > .env
    - echo "APP_ENV=testing" >> .env
    - echo "APP_KEY=" >> .env
    - echo "APP_DEBUG=true" >> .env
    - echo "APP_URL=http://localhost" >> .env
    - echo "" >> .env
    - echo "DB_CONNECTION=mysql" >> .env
    - echo "DB_HOST=mysql" >> .env
    - echo "DB_PORT=3306" >> .env
    - echo "DB_DATABASE=library_db" >> .env
    - echo "DB_USERNAME=laravel" >> .env
    - echo "DB_PASSWORD=password" >> .env
    - echo "" >> .env
    - echo "CACHE_DRIVER=array" >> .env
    - echo "QUEUE_CONNECTION=sync" >> .env
    - echo "SESSION_DRIVER=array" >> .env
    - echo "Current .env content:"
    - cat .env
    - php artisan key:generate --force --ansi
    - echo "After key generate:"
    - cat .env | grep APP_KEY
    - php artisan config:clear
    - php artisan config:cache
    - echo "Running migrations..."
    - php artisan migrate:fresh --seed --force
    - echo "Running tests..."
    - mkdir -p storage/test-results
    - chmod -R 777 storage
    - vendor/bin/phpunit --log-junit=storage/test-results/test-results.xml || true
  artifacts:
    when: always
    reports:
      junit: storage/test-results/test-results.xml
    paths:
      - storage/logs/
      - storage/test-results/
    expire_in: 1 week
  only:
    - main
    - dev

test:coverage:
  image: php:8.2-fpm
  stage: test
  services:
    - name: mysql:8.0
      alias: mysql
      command:
        ["--default-authentication-plugin=mysql_native_password", "--skip-ssl"]
  script:
    - echo "Waiting for MySQL to be ready..."
    - |
      for i in {1..30}; do
        if mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "SELECT 1" &>/dev/null; then
          echo "MySQL is ready!"
          break
        fi
        echo "Waiting for MySQL..."
        sleep 2
      done
    - echo "Setting up database and user..."
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "CREATE DATABASE IF NOT EXISTS library_db;"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "DROP USER IF EXISTS 'laravel'@'%';"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "CREATE USER 'laravel'@'%' IDENTIFIED WITH mysql_native_password BY 'password';"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "GRANT ALL PRIVILEGES ON *.* TO 'laravel'@'%' WITH GRANT OPTION;"
    - mysql -hmysql -uroot -p$MYSQL_ROOT_PASSWORD --ssl=0 -e "FLUSH PRIVILEGES;"
    - echo "Setting up Laravel environment..."
    - rm -f .env
    - echo "APP_NAME=LibraryManagement" > .env
    - echo "APP_ENV=testing" >> .env
    - echo "APP_KEY=" >> .env
    - echo "APP_DEBUG=true" >> .env
    - echo "APP_URL=http://localhost" >> .env
    - echo "" >> .env
    - echo "DB_CONNECTION=mysql" >> .env
    - echo "DB_HOST=mysql" >> .env
    - echo "DB_PORT=3306" >> .env
    - echo "DB_DATABASE=library_db" >> .env
    - echo "DB_USERNAME=laravel" >> .env
    - echo "DB_PASSWORD=password" >> .env
    - echo "" >> .env
    - echo "CACHE_DRIVER=array" >> .env
    - echo "QUEUE_CONNECTION=sync" >> .env
    - echo "SESSION_DRIVER=array" >> .env
    - php artisan key:generate --force --ansi
    - php artisan config:clear
    - php artisan config:cache
    - php artisan migrate:fresh --seed --force
    - echo "Installing Xdebug for coverage..."
    - pecl install xdebug
    - docker-php-ext-enable xdebug
    - echo "xdebug.mode=coverage" >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini
    - echo "Running tests with coverage..."
    - vendor/bin/phpunit --coverage-text --coverage-cobertura=coverage.cobertura.xml
  coverage: '/^\s*Lines:\s*\d+.\d+\%/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.cobertura.xml
  only:
    - main
    - dev
  allow_failure: true

deploy:staging:
  image: php:8.2-fpm
  stage: deploy
  environment:
    name: staging
    url: https://staging.library-management.com
    on_stop: stop:staging
  script:
    - echo "Deploying to STAGING environment"
    - echo "Application - $CI_PROJECT_NAME"
    - echo "Branch - $CI_COMMIT_REF_NAME"
    - echo "Commit - $CI_COMMIT_SHORT_SHA"
    - echo "Database - $DB_DATABASE"
    - echo "Staging deployment completed"
  only:
    - dev
  when: manual

deploy:production:
  image: php:8.2-fpm
  stage: deploy
  environment:
    name: production
    url: https://library-management.com
    on_stop: stop:production
  script:
    - echo "Deploying to PRODUCTION environment"
    - echo "Application - $CI_PROJECT_NAME"
    - echo "Branch - $CI_COMMIT_REF_NAME"
    - echo "Commit - $CI_COMMIT_SHORT_SHA"
    - echo "Production deployment completed"
  only:
    - main
  when: manual

stop:staging:
  image: php:8.2-fpm
  stage: deploy
  environment:
    name: staging
    action: stop
  script:
    - echo "Stopping staging environment"
  when: manual
  only:
    - dev

stop:production:
  image: php:8.2-fpm
  stage: deploy
  environment:
    name: production
    action: stop
  script:
    - echo "Stopping production environment"
  when: manual
  only:
    - main
