# Docker image untuk CI/CD
image: php:8.2-cli

# Stages dalam pipeline
stages:
  - test
  - deploy

# Variables global
variables:
  MYSQL_ROOT_PASSWORD: rootpassword
  MYSQL_DATABASE: library_db
  MYSQL_USER: root
  MYSQL_PASSWORD: rootpassword
  DB_CONNECTION: mysql
  DB_HOST: mysql
  DB_PORT: 3306
  DB_DATABASE: library_db
  DB_USERNAME: root
  DB_PASSWORD: rootpassword

# Cache dependencies
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - vendor/

# Before script - install dependencies
before_script:
  - apt-get update -yqq
  - apt-get install -yqq git libzip-dev libpng-dev libonig-dev libxml2-dev unzip curl default-mysql-client
  - docker-php-ext-install pdo_mysql mbstring zip exif pcntl bcmath gd
  - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
  - composer install --no-interaction --prefer-dist --optimize-autoloader

test:unit:
  stage: test
  services:
    - name: mysql:8.0
      command: ["--default-authentication-plugin=mysql_native_password"]
  script:
    # Wait for MySQL to be ready
    - echo "Waiting for MySQL to be ready..."
    - |
      for i in {1..30}; do
        if mysql -h mysql -u root -p$MYSQL_ROOT_PASSWORD -e "SELECT 1" &>/dev/null; then
          echo "MySQL is ready!"
          break
        fi
        echo "Waiting for MySQL... ($i/30)"
        sleep 2
      done

    # Create database and user
    - echo "Setting up database and user..."
    - |
      mysql -h mysql -u root -p$MYSQL_ROOT_PASSWORD -e "
        CREATE DATABASE IF NOT EXISTS library_db;
        CREATE USER IF NOT EXISTS 'laravel'@'%' IDENTIFIED BY 'password';
        GRANT ALL PRIVILEGES ON library_db.* TO 'laravel'@'%';
        FLUSH PRIVILEGES;
      "

    # Setup Laravel environment
    - cp .env.example .env
    - sed -i "s/DB_HOST=db/DB_HOST=mysql/" .env
    - sed -i "s/DB_USERNAME=laravel/DB_USERNAME=laravel/" .env
    - sed -i "s/DB_PASSWORD=password/DB_PASSWORD=password/" .env
    - php artisan key:generate
    - php artisan config:clear

    # Verify connection
    - echo "Verifying database connection..."
    - php artisan migrate:status || echo "Migrations table not found (expected)"

    # Run migrations
    - echo "Running migrations..."
    - php artisan migrate:fresh --seed --force

    # Create directory and run tests
    - mkdir -p storage/test-results
    - chmod -R 777 storage
    - echo "Running tests..."
    - vendor/bin/phpunit --log-junit=$(pwd)/storage/test-results/test-results.xml || true

  artifacts:
    when: always
    reports:
      junit: storage/test-results/test-results.xml
    paths:
      - storage/logs/
      - storage/test-results/
  only:
    - main
    - dev

test:coverage:
  stage: test
  services:
    - name: mysql:8.0
      command: ["--default-authentication-plugin=mysql_native_password"]
  script:
    # Wait for MySQL
    - |
      for i in {1..30}; do
        if mysql -h mysql -u root -p$MYSQL_ROOT_PASSWORD -e "SELECT 1" &>/dev/null; then
          echo "MySQL is ready!"
          break
        fi
        echo "Waiting for MySQL... ($i/30)"
        sleep 2
      done

    # Create database and user
    - |
      mysql -h mysql -u root -p$MYSQL_ROOT_PASSWORD -e "
        CREATE DATABASE IF NOT EXISTS library_db;
        CREATE USER IF NOT EXISTS 'laravel'@'%' IDENTIFIED BY 'password';
        GRANT ALL PRIVILEGES ON library_db.* TO 'laravel'@'%';
        FLUSH PRIVILEGES;
      "

    # Setup environment
    - cp .env.example .env
    - sed -i "s/DB_HOST=db/DB_HOST=mysql/" .env
    - sed -i "s/DB_USERNAME=laravel/DB_USERNAME=laravel/" .env
    - sed -i "s/DB_PASSWORD=password/DB_PASSWORD=password/" .env
    - php artisan key:generate
    - php artisan config:clear
    - php artisan migrate:fresh --seed --force

    # Install Xdebug
    - pecl install xdebug
    - docker-php-ext-enable xdebug
    - echo "xdebug.mode=coverage" >> /usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini

    # Run tests with coverage
    - php artisan test --coverage-text

  coverage: '/^\s*Lines:\s*\d+.\d+\%/'
  only:
    - main
    - dev
  allow_failure: true

deploy:staging:
  stage: deploy
  environment:
    name: staging
    url: https://staging.library-management.com
    on_stop: stop:staging
  script:
    - echo "Deploying to STAGING environment"
    - echo "Application - $CI_PROJECT_NAME"
    - echo "Branch - $CI_COMMIT_REF_NAME"
    - echo "Commit - $CI_COMMIT_SHORT_SHA"
    - echo "Database - $DB_DATABASE"
    - echo "Staging deployment completed"
  only:
    - dev
  when: manual

deploy:production:
  stage: deploy
  environment:
    name: production
    url: https://library-management.com
    on_stop: stop:production
  script:
    - echo "Deploying to PRODUCTION environment"
    - echo "Application - $CI_PROJECT_NAME"
    - echo "Branch - $CI_COMMIT_REF_NAME"
    - echo "Commit - $CI_COMMIT_SHORT_SHA"
    - echo "Production deployment completed"
  only:
    - main
  when: manual

stop:staging:
  stage: deploy
  environment:
    name: staging
    action: stop
  script:
    - echo "Stopping staging environment"
  when: manual
  only:
    - dev

stop:production:
  stage: deploy
  environment:
    name: production
    action: stop
  script:
    - echo "Stopping production environment"
  when: manual
  only:
    - main
